{% extends 'dashboard/dashboard_rapport_veterinaire_animal/dashboardReport.html.twig' %}

{% block action %}
{% if is_granted('ROLE_VETO') %}
<div class="accordion-item">
    <h2 class="accordion-header">
        <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseTwo" aria-expanded="false" aria-controls="flush-collapseTwo">
        Rédiger un nouveau rapport sur un animal
        </button>
    </h2>
    <div id="flush-collapseTwo" class="accordion-collapse collapse bg-dark show" data-bs-parent="#accordionFlushExample">
        <div class="accordion-body">
            <div class="row justify-content-center">
                <div class="col-12">
                    {{ form_start(form) }}
                        {{ form_errors(form) }}
                        <div class="form-group">
                            {{ form_label(form.date) }}
                            {{ form_widget(form.date) }}
                            <div class="form-error">
                                {{ form_errors(form.date) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(form.animal) }}
                            {{ form_widget(form.animal) }}
                            <div class="form-error">
                                {{ form_errors(form.animal) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(form.etat) }}
                            {{ form_widget(form.etat) }}
                            <div class="form-error">
                                {{ form_errors(form.etat) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(form.rapport) }}
                            {{ form_widget(form.rapport) }}
                            <div class="form-error">
                                {{ form_errors(form.rapport) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(form.nourriture) }}
                            {{ form_widget(form.nourriture) }}
                            <div class="form-error">
                                {{ form_errors(form.nourriture) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(form.quantite_nourriture) }}
                            {{ form_widget(form.quantite_nourriture) }}
                            <div class="form-error">
                                {{ form_errors(form.quantite_nourriture) }}
                            </div>
                        </div>
                        <div class="col-12 text-center">    
                        <div class="form-group">
                            {{ form_row(form.submit) }}
                        </div>
                </div>    
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endif %}
{% endblock %}